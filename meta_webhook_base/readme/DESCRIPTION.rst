One shared, bounded and credential-safe webhook ingress for a Meta App, with
immutable sanitized evidence, extensible consumer dispatch, fenced Page Graph
runtimes and periodic subscription reconciliation.
